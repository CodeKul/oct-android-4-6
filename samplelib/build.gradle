apply plugin: 'java'

dependencies {
    compile fileTree(dir: 'libs', include: ['*.jar'])
}

task codeRun(type: JavaExec) {
    classpath sourceSets.main.runtimeClasspath
    main = "com.codekul.samplelib.Lib"
}